{#
/**
 * @file
 * Icon component.
 * Creates an inline SVG which references an icon symbol in a spritesheet pack.
 * E.g.
 *
 * {{spritesheet}}        {{symbol}}
 * /icons/civic-arrow.svg#arrows-caret
 *
 * Uses the "arrows-caret" symbol from the "/icons/civic-arrow.svg" spritesheet.
 *
 * Variables:
 * - color: [string] Icon color.
 * - symbol: [string] Icon name.
 * - modifier_class: [string] Additional classes to modify component styling.
 */
#}
{% extends './icon_library.twig' %}
{% block content %}
  {% set icon = icons[symbol] %}
  <svg
    width="{{icon.width}}"
    height="{{icon.height}}"
    class="civic-icon{{ color ? ' civic-icon--color-' ~ color }}{{ modifier_class ? ' ' ~ modifier_class}}"
    viewBox="0 0 {{icon.width}} {{icon.height}}"
    xmlns="http://www.w3.org/2000/svg"
  >
    {% for path in icon.paths %}
      <path d="{{path}}" />
    {% endfor %}
  </svg>
{% endblock %}
