{#
/**
 * @file
 * Promo component.
 *
 * Variables:
 * - theme: [string] Theme: light, dark.
 * - title: [string] Title text.
 * - summary: [string] Summary.
 * - link: [object] Link:
 *   - title [string] Link title.
 *   - url [string] Link URL.
 *   - is_new_window [boolean] Open link in the new window.
 *   - is_external: [boolean] Link is external.
 * - vertical_spacing: [string] With top, bottom or both vertical spaces.
 * - attributes: [string] Additional attributes.
 * - modifier_class: [string] Additional classes.
 */
#}

{% set vertical_spacing_class = vertical_spacing in ['top', 'bottom', 'both'] ? 'ct-vertical-spacing--%s'|format(vertical_spacing) : '' %}
{% set theme_class = 'ct-theme-%s'|format(theme|default('light')) %}
{% set modifier_class = '%s %s %s'|format(theme_class, vertical_spacing_class, modifier_class|default('')) %}

{% if title is not empty or summary is not empty %}
  <div class="ct-promo {{ modifier_class }}" {% if attributes is not empty %}{{ attributes|raw }}{% endif %}>
    <div class="container">
      <div class="row">
        <div class="col-xxs-12">
          {% block content_top %}
            {% if content_top is not empty %}
              <div class="ct-next-steps__content-top">
                {{ content_top }}
              </div>
            {% endif %}
          {% endblock %}

          {% block content %}
            <div class="ct-promo__inner">
              {% block content_left %}
                <div class="ct-promo__content">
                  {% if title %}
                    {% include '@atoms/heading/heading.twig' with {
                      content: title,
                      level: 4,
                      theme: theme,
                      modifier_class: 'ct-promo__content__title',
                    } only %}
                  {% endif %}

                  {% if summary is not empty %}
                    {% include '@atoms/paragraph/paragraph.twig' with {
                      theme: theme,
                      content: summary,
                      allow_html: true,
                      modifier_class: 'ct-promo__content__summary',
                    } only %}
                  {% endif %}
                </div>
              {% endblock %}

              {% block content_right %}
                {% if link %}
                  <div class="ct-promo__links">
                    {% include '@atoms/button/button.twig' with {
                      theme: theme,
                      type: 'secondary',
                      kind: 'link',
                      size: 'large',
                      text: link.text,
                      url: link.url,
                      is_new_window: link.is_new_window is defined ? link.is_new_window : false,
                      is_external: link.is_external is defined ? link.is_external : false,
                      modifier_class: 'ct-promo__links__button',
                    } only %}
                  </div>
                {% endif %}
              {% endblock %}
            </div>
          {% endblock %}

          {% block content_bottom %}
            {% if content_bottom is not empty %}
              <div class="ct-next-steps__content-bottom">
                {{ content_bottom }}
              </div>
            {% endif %}
          {% endblock %}
        </div>
      </div>
    </div>
  </div>
{% endif %}
