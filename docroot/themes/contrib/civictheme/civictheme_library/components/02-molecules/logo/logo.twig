{#
/**
 * @file
 * Logo component.
 *
 * Variables:
 * - theme: [string] Theme: light, dark.
 * - type: [string] Type: default, inline, stacked.
 * - logos: [object] Object with 'mobile' and 'desktop' keys:
 *    - primary:
 *       - src: [string] URL for the logo.
 *       - alt: [string] Image alt text.
 *    - secondary:
 *       - src: [string] URL for the logo.
 *       - alt: [string] Image alt text.
 * - url: [string] Optional URL that wraps the logo.
 * - title: [string] Optional logo title attribute.
 * - attributes: [string] Additional attributes.
 * - modifier_class: [string] Additional classes.
 */
#}

{% set type = type in ['default', 'inline', 'stacked'] ? type : 'default' %}
{% set theme_class = 'civictheme-theme-%s'|format(theme|default('light')) %}
{% set modifier_class = '%s %s'|format(theme_class, modifier_class|default('')) %}

{% if logos is not empty %}
  {% for logo_type, logo in logos %}
    {% set visibility_class = logo_type == 'mobile' ? 'show-xxs hide-m' : 'hide-xxs show-m' %}
    {% if logo.secondary.src is not empty %}
      {% if type in ['inline', 'stacked'] %}
        {% set type_class = 'civictheme-logo-type-%s'|format(type|default('default')) %}
        {% set logo_secondary = TRUE %}
      {% endif %}
    {% endif %}
    {% if logo.primary.src is not empty %}
      <div class="civictheme-logo {{ visibility_class }} {{ type_class }} {{ modifier_class }}" {% if attributes is not empty %}{{ attributes|raw }}{% endif %}>
        {% if url is not empty %}
        <a class="civictheme-logo__url" href="{{ url }}" title="{{ title|default('Click to go to the homepage') }}">
        {% endif %}
          {% if logo_secondary %}
            {% include '@atoms/image/image.twig' with {
              theme: theme,
              src: logo.secondary.src,
              alt:  logo.secondary.alt,
            } only %}
            <hr class="civictheme-logo--hr">
          {% endif %}
          {% include '@atoms/image/image.twig' with {
            theme: theme,
            src: logo.primary.src,
            alt:  logo.primary.alt,
          } only %}
        {% if url is not empty %}
        </a>
        {% endif %}
      </div>
    {% endif %}
  {% endfor %}
{% endif %}
