{#
/**
 * @file
 * Checkbox component.
 *
 * Variables:
 * - theme: [string] Theme: light, dark.
 * - content: [string] Input field label.
 * - value: [string] Input field value.
 * - disabled: [boolean] Disabled state.
 * - required: [boolean] Required state.
 * - has_error: [boolean] Error state.
 * - attributes: [string] Additional attributes.
 * - modifier_class: [string] Additional classes.
 */
#}

{% set theme_class = 'civictheme-theme-%s'|format(theme|default('light')) %}
{% set disabled_class = disabled ? 'civictheme-checkbox--disabled' : '' %}
{% set required_class = required ? 'civictheme-checkbox--required' : '' %}
{% set error_class = has_error ? 'civictheme-checkbox--error' : '' %}
{% set modifier_class = '%s %s %s %s %s'|format(theme_class, disabled_class, required_class, error_class, modifier_class|default('')) %}

<div class="civictheme-checkbox {{ modifier_class|trim }}">
  <input class="civictheme-checkbox__element" type="checkbox" value="{{ value }}" {% if attributes %}{{ attributes|raw }}{% endif %} {{ disabled ? 'disabled' }} {{ required ? 'required' }}/>
  <span class="civictheme-checkbox__state"></span>
  {% if content is not empty %}
    <label class="civictheme-checkbox__label{{ disabled ? ' civictheme-checkbox__label--disabled' }}">{{ content }}</label>
  {% endif %}
</div>
