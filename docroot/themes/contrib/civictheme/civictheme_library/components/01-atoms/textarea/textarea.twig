{#
/**
 * @file
 * Textarea component.
 *
 * Variables:
 * - theme: [string] Theme: light, dark.
 * - placeholder: [string] Placeholder text.
 * - disabled: [boolean] Disabled or not.
 * - required: [boolean] Required or not.
 * - rows: [integer] Optional number of rows.
 * - attributes: [string] Additional attributes.
 * - modifier_class: [string] Additional classes.
 */
#}

{% set theme_class = 'ct-theme-%s'|format(theme|default('light')) %}
{% set disabled_class = disabled ? 'ct-input--disabled disabled' : '' %}
{% set required_class = required ? 'ct-input--required required' : '' %}
{% set modifier_class = '%s %s %s %s'|format(theme_class, disabled_class, required_class, modifier_class|default('')) %}

{% set rows = rows|default(5) %}

<textarea class="ct-textarea ct-input__element {{ modifier_class }}" {% if attributes %}{{ attributes|raw }}{% endif %} {% if placeholder %} placeholder="{{ placeholder }}" {% endif %} {{ disabled ? 'disabled' }} {{ required ? 'required' }} rows="{{ rows }}">{{ value }}</textarea>
