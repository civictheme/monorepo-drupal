{#
/**
 * @file
 * Civic theme implementation for a 'fieldset' dropdown form filter.
 *
 * This is separate from fieldsets that are used to contain filters. This is for fieldsets to be included inside
 * a dropdown filter. It is used for date range and other fields that we might want to group in a dropdown.
 *
 * Available variables
 * - attributes: A list of HTML attributes for the wrapper element.
 * - children: The form elements.
 *
 * @see template_preprocess_checkboxes()
 *
 * @ingroup themeable
 */
#}
{% include '@molecules/dropdown-filter/dropdown-filter.twig' with {
  options: children,
} %}
