{#
/**
 * @file
 * CivicTheme Textfield component.
 *
 * Props:
 * - theme: [string] Theme variation (light or dark).
 * - name: [string] DOM name (required).
 * - id: [string] DOM ID attribute.
 * - value: [string] The textfield value.
 * - placeholder: [string] Placeholder text.
 * - is_invalid: [boolean] Whether the textfield is invalid.
 * - is_disabled: [boolean] Whether the textfield is disabled.
 * - is_required: [boolean] Whether the textfield is required.
 * - attributes: [Drupal\Core\Template\Attribute] Additional HTML attributes.
 * - modifier_class: [string] Additional CSS classes.
 */
#}

{% set theme_class = 'ct-theme-%s'|format(theme|default('light')) %}
{% set is_invalid_class = is_invalid ? 'ct-textfield--is-invalid' : '' %}
{% set modifier_class = '%s %s %s'|format(theme_class, is_invalid_class, modifier_class|default(''))|trim %}

{% if name is not empty %}
  <input
    type="text"
    class="ct-textfield {{ modifier_class -}}"
    name="{{ name }}"
    {% if value is not empty %}value="{{ value }}"{% endif %}
    {% if id %}id="{{ id }}"{% endif %}
    {% if is_disabled %}disabled{% endif %}
    {% if is_required %}required{% endif %}
    {% if is_invalid %}aria-invalid="true"{% endif %}
    {% if placeholder %}placeholder="{{ placeholder }}"{% endif %}
    {% if attributes is defined and attributes is not null %}{{- attributes -}}{% endif %}
  />
{% endif %}
