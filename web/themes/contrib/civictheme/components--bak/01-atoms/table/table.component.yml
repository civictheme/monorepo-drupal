$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: Table
status: stable
description: Table component for tabular data with styling options
props:
  type: object
  properties:
    theme:
      type:
      - string
      - 'null'
      - integer
      title: Theme
      description: Theme variation (light or dark).
      enum:
      - light
      - dark
      - ''
      - null
    caption:
      type:
      - string
      - 'null'
      - integer
      title: Caption
      description: Table caption.
    caption_position:
      type:
      - string
      - 'null'
      - integer
      title: Caption position
      description: Caption position (before, after).
      enum:
      - before
      - after
      - ''
      - null
      default: before
    header:
      type: array
      title: Header
      description: Header cells.
      items:
        type: string
    header_sanitized:
      type: array
      title: Header
      description: Provide no-HTML header column names. Used for mobile header labels.
      items:
        type: string
    rows:
      type: array
      title: Rows
      description: Table rows.
      items:
        type: array
        items:
          type: string
    footer:
      type: array
      title: Footer
      description: Footer cells.
      items:
        type: string
    is_striped:
      type:
      - boolean
      - 'null'
      - string
      - integer
      title: Striped
      description: Whether to show table with striped rows.
    is_data_table:
      type:
      - boolean
      - 'null'
      - string
      - integer
      title: Data table
      description: Whether to show as a data table.
    modifier_class:
      type:
      - string
      - 'null'
      - integer
      title: Modifier classes
      description: Additional CSS classes.
