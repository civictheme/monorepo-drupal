// phpcs:ignoreFile
import Component from './spacing.stories.twig';

const meta = {
  title: 'Base/Spacing',
  component: Component,
};

export default meta;

export const Spacing = {
  parameters: {
    html: {
      disable: true,
    },
  },
};
