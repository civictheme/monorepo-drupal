{#
/**
 * @file
 * CivicTheme implementation to display a one-column contained layout.
 *
 * Available variables:
 * - content: The content for this layout.
 * - is_contained: Whether the content should be contained
 * - modifier_class: CSS modifying class
 * - region_attributes: HTML attributes for the layout <div>.
 *
 * @ingroup themeable
 */
#}

{% if content %}
  <div class="layout">
    <div {{ region_attributes.content.addClass('layout__region', 'layout__region--content') }}>
      <div class="ct-layout--single-column">
        <div class="container">
          <div class="row">
            {% block content %}
              <div class="ct-layout__content col-m-12 col-xxs-12 {{ modifier_class }}">
                {{ content }}
              </div>
            {% endblock %}
          </div>
        </div>
      </div>
    </div>
  </div>
{% endif %}
