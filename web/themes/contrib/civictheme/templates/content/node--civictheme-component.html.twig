{#
/**
 * @file
 * CivicTheme implementation for nodes rendered as SDC components.
 *
 * Used for all non-layout-builder view modes where the preprocess builds
 * the entire output as an SDC render array in $content.
 */
#}
{{ content }}
