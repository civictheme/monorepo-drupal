{#
/**
 * @file
 * CivicTheme implementation to display a page title.
 */
#}
{{ title_prefix }}
{% if title %}
  {% include 'civictheme:heading' with {
    theme: theme|default('light'),
    content: title,
    level: '1',
  } only %}
{% endif %}
{{ title_suffix }}
